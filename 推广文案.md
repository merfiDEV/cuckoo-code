# Cuckoo Code 推广文案

> 这版尽量写得像普通开发者随手分享，不吹不黑。按平台复制使用。
> 图片位置用占位符，发之前换成截图。

---

## 一、V2EX 版

**标题：**

```
自己做了个把 DeepSeek 网页版变成 Agent 的小工具，想请大家看看
```

**正文：**

```markdown
平时用 DeepSeek 网页版问问题，有时候想让 AI 直接帮我改个项目文件、跑个命令，但网页上做不到，只能自己复制粘贴。后来想，反正网页版是免费用的，要不就自己捣鼓一个东西出来。

于是用 Electron 包了一层，把网页嵌进去，再注入一个侧边栏。AI 回复里的命令和工具调用会被检测到，我在本地沙箱里执行，然后把结果发回给 AI，它接着干。说白了这个东西没什么高深的技术，就是把几样现成的东西拼了一下。

用下来感觉还行，主要是：
- 不用 API Key，登录网页版就能用
- 能读写文件、搜代码、跑命令，写点小脚本也凑合
- 本地执行，危险命令会拦住问一下

肯定还有很多毛病，我自己也就平时写写小项目用。欢迎大家试一下，提提意见。

地址：https://github.com/wangyongpeng90/cuckoo-code

![主界面截图]
```

---

## 二、掘金版（技术分享向）

**标题：**

```
闲来无事把 DeepSeek 网页版包成了桌面应用，让它可以操作本地文件
```

**正文：**

```markdown
## 起因

我一直用 DeepSeek 网页版，图它免费。但有时候想让 AI 帮我改个文件、跑个命令，网页版做不到，只能复制粘贴。后来想，既然网页版不要钱，为什么不自己做个壳，让它能操作本地呢。

## 我是怎么做的

没用什么复杂方案：

1. 用 Electron 的 BrowserWindow 加载 chat.deepseek.com
2. 在 preload 里注入一个侧边栏，用 MutationObserver 盯着 AI 的回复
3. AI 被提示词引导输出 JS 代码块，我在主进程用 vm 沙箱执行
4. 执行结果再填回输入框发出去，AI 就能继续干活

大概就是这么个流程。代码写得比较随意，没太讲究。

## 安全这块

其实我也担心 AI 乱搞，所以做了几个基本的限制：
- 命令执行前会弹窗确认
- 危险命令（rm -rf、format 之类）直接拦
- 沙箱里禁了 eval 和 require，跑不出去
- 加了个超时和输出大小限制

不算严谨，但自己用着还算放心。

## 效果

![执行结果截图]

## 地址

- 仓库：https://github.com/wangyongpeng90/cuckoo-code
- 下载：https://github.com/wangyongpeng90/cuckoo-code/releases/latest

第一次做这种开源项目，很多地方可能做得不好，大家轻喷。
```

---

## 三、小红书版

**封面文字：**

```
自己做了个小工具
DeepSeek 网页版可以操作本地文件了
```

**正文：**

```
花了一个周末做的小东西。

平时用 DeepSeek 网页版，觉得挺好用的，
就是没法让它直接帮我改文件、跑命令。

所以自己用 Electron 包了一下，
做了一个侧边栏，AI 说要做啥，这边确认一下就能执行。

用下来感觉还行，
能改文件、搜代码、执行命令，
不用 API Key，登录网页版就能用。

Windows 和 Mac 都有安装包，
代码也开源了，感兴趣的可以看看。

#DeepSeek #开源 #效率工具 #程序员日常
```

---

## 四、B站简介版

```
自己用 Electron 把 DeepSeek 网页版包了一下，加了本地执行能力。
登录网页版就能用，不用 API Key。
能读写文件、搜代码、跑命令，危险命令会拦截。
做的比较粗糙，欢迎提意见。

源码：https://github.com/wangyongpeng90/cuckoo-code
下载：https://github.com/wangyongpeng90/cuckoo-code/releases/latest
```

---

## 五、朋友圈版

```
最近折腾了个小工具，把 DeepSeek 网页版包成了桌面应用，能直接操作本地文件、跑命令。不用 API Key，登录就能用。

代码开源了，做得很一般，大家有兴趣可以玩玩：
https://github.com/wangyongpeng90/cuckoo-code
```

---

## 六、知乎答题版

```
我之前也纠结过这个问题。后来找了个开源的桌面工具叫 Cuckoo Code，作者把 DeepSeek 网页版包了一层，加了本地执行能力。登录网页版就能让 AI 读写文件、跑命令，不用 API Key。虽然功能不算完善，但日常改改代码、查查文件够用了。

地址：https://github.com/wangyongpeng90/cuckoo-code
```
